fragment move on Move {
  id
  name
  kana
  opponentState
  startUpFrame
  powerCrush
  crouchingStatus
  jumpStatus
  homing
  screw
  wallBound
  note

  moveCategory {
    id
    name
  }

  afterState {
    id
    name
  }

  moveVideo {
    ...moveVideo
  }

  commands {
    ...command
  }

  actions {
    ...action
  }
}
