query PageDashboardMoveEdit($moveId: ID!) {
  move(moveId: $moveId) {
    ...move

    moveCategory {
      id
      name

      character {
        slug
        name

        states {
          id
          name
        }
      }
    }
  }
}
