query MoveSelectOptions($characterSlug: String!) {
  character(characterSlug: $characterSlug) {
    id

    moveCategories {
      id
      name

      moves {
        id
        name
        command
      }
    }
  }
}
