fragment TournamentForm on Tournament {
  organizerId
  name
  url
  streamingUrl
  startsAt
  description
}

query TournamentForm {
  organizers(per: 100) {
    records { ...OrganizerSelectOption }
  }
}