query TopPage {
  tournaments(thisWeek: true, first: 3) {
    nodes { ...TournamentCard }
  }

  players(per: 4) {
    records { ...PlayerCard }
  }

  characters(order: use_rate, per: 4) {
    records { ...CharacterCard }
  }

  articles(per: 3) {
    records { ...ArticleCard }
  }
}
