query PlayerStandingsPage($playerSlug: String!) {
  player(playerSlug: $playerSlug) {
    ...PlayerBreadcrumbs
    ...PlayerProfile
    ...PlayerTabs

    standings(first: 10) {
      nodes {
        ...PlayerStandingCard
      }
    }
  }
}
