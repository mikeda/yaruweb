query CharacterPage($characterSlug: String!) {
  character(characterSlug: $characterSlug) {
    ...CharacterBreadcrumbs
    ...CharacterProfile
    ...CharacterTabs

    story
    description
  }

  battleCounts(characterSlug: $characterSlug, first: 10) {
    nodes { ...PlayerBattleCountChip }
  }
}
