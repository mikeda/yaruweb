query PageTournament($tournamentId: ID!) {
  tournament(tournamentId: $tournamentId) {
    id
    name
    mainImageUrl
    url
    streamingUrl
    description
    startsAt

    winnings {
      ...WinningCard
    }

    videos {
      ...TournamentVideoCard
    }
  }
}