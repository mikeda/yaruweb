query ComboSelectOptions($characterSlug: String!) {
  character(characterSlug: $characterSlug) {
    id

    comboCategories {
      id
      name

      combos {
        id
        command
      }
    }
  }
}
