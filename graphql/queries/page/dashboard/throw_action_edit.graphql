query PageDashboardThrowActionEdit($actionId: ID!) {
  throwAction(actionId: $actionId) {
    ...throwAction

    move {
      id
      name

      moveCategory {
        id
        name

        character {
          slug
          name
        }
      }
    }
  }
}
