fragment TournamentBattleForm on TournamentBattle {
  id
  tournamentVideo { id }
  startSec
  round
  leftPlayer { slug }
  rightPlayer { slug }
  leftCharacter { slug }
  rightCharacter { slug }
  leftRounds
  rightRounds
}
