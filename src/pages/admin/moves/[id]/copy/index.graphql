query PageAdminMoveCopy($moveId: ID!) {
  move(moveId: $moveId) {
    ...MoveForm

    moveCategory {
      id
      name

      character {
        slug
        name
      }

      moves {
        ...MovePositionSelect
      }
    }
  }
}
