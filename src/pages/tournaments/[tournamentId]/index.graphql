query PageTournament($tournamentId: ID!) {
  tournament(tournamentId: $tournamentId) {
    id
    name
    mainImageUrl
    url
    streamingUrl
    description
    organizerName
    organizerTwitterId
    startsAt

    rankings {
      id
    }

    videos {
      ...TournamentVideoCard
    }
  }
}