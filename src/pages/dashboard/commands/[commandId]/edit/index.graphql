query PageDashboardCommandEdit($commandId: ID!) {
  command(commandId: $commandId) {
    ...command

    operations {
      id
    }

    move {
      id
      name

      moveCategory {
        id
        name

        character {
          slug
          name
        }
      }
    }
  }
}
