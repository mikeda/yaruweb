query CharacterBattlesPageBattles($characterSlug: String!, $playerSlug: String, $page: Int!) {
  battles(characterSlug: $characterSlug, playerSlug: $playerSlug, page: $page, per: 10) {
    records {
      ...BattleListItem
    }

    paging {
      ...paging
    }
  }
}
