query PageDashboardCharacter($characterSlug: ID!) {
  character(characterSlug: $characterSlug) {
    slug
    longName

    moveCategories {
      id
      name
      movesCount
    }

    comboCategories {
      id
      name
      combosCount
    }
  }
}
