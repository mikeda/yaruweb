query MoveSelectOptions($characterSlug: ID!) {
  moveCategories(characterSlug: $characterSlug) {
    id
    name

    moves {
      id
      name

      commands {
        operations {
          key
        }
      }
    }
  }
}
