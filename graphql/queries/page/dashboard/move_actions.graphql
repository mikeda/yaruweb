query PageDashboardMoveActions($moveId: ID!) {
  move(moveId: $moveId) {
    id
    name

    actions {
      ...action
    }

    moveCategory {
      id
      name

      character {
        slug
        name
      }
    }
  }
}
