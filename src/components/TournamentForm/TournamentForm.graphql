fragment TournamentForm on Tournament {
  organizerId
  league { id }
  name
  url
  streamingUrl
  startsAt
  description
}

query TournamentForm {
  organizers(per: 100) {
    records { ...OrganizerSelectOption }
  }
  leagues { ...LeagueSelectOption }
}