query MyArticle($articleId: ID!) {
  myArticle(articleId: $articleId) {
    id
    title
    description
    mainImageUrl
    publishedAt
    faved
    favsCount
    status
    category
    content

    author {
      name
      avatarUrl
    }
  }
}
