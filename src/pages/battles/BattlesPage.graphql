query BattlesPage {
  players(per: 10) {
    records { ... PlayerChip }
  }

  characters(order: use_rate, per: 10) {
    records { ...CharacterChip }
  }

  battles(per: 10) {
    records { ...BattleListItem }
    paging { ...paging }
  }
}

query BattlesPageBattles($page: Int!) {
  battles(page: $page, per: 10) {
    records { ...BattleListItem }
    paging { ...paging }
  }
}
