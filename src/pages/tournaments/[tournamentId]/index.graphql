query PageTournament($tournamentId: ID!) {
  tournament(tournamentId: $tournamentId) {
    id
    name
    mainImageUrl
    url
    streamingUrl
    description
    startsAt

    standings {
      ...StandingCard
    }

    videos {
      ...TournamentVideoCard
    }
  }
}