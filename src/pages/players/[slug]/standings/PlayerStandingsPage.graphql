query PlayerStandingsPage($playerSlug: String!) {
  player(playerSlug: $playerSlug) {
    ...PlayerBreadcrumbs
    ...PlayerProfile
    ...PlayerTabs
  }

  standings(playerSlug: $playerSlug, first: 10) {
    nodes { ...PlayerStandingCard }
  }
}
