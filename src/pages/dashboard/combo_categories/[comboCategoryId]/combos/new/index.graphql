query PageDashboardComboNew($comboCategoryId: ID!) {
  comboCategory(comboCategoryId: $comboCategoryId) {
    id
    name

    character {
      slug
      name

      moveCategories {
        id
        name

        moves {
          id
          name
        }
      }
    }
  }
}
