mutation CreateComboCategory($characterId: ID!, $attributes: ComboCategoryAttributes!) {
  createComboCategory(input: { characterId: $characterId, attributes: $attributes }) {
    comboCategory {
      id
    }
  }
}