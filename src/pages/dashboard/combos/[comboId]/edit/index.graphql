query PageDashboardComboEdit($comboId: ID!) {
  combo(comboId: $comboId) {
    ...combo

    operations {
      id
    }

    comboCategory {
      id
      name

      character {
        slug
        name
      }
    }
  }
}
