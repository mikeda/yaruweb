query PageTournamentVideo($tournamentVideoId: ID!) {
  tournamentVideo(tournamentVideoId: $tournamentVideoId) {
    ...tournamentVideo

    tournament {
      id
      name
    }

    battles {
      id
      round
      startSec

      leftPlayer { name }
      rightPlayer { name }
    }
  }
}
