query PageMoveCategory($moveCategoryId: ID!) {
  moveCategory(moveCategoryId: $moveCategoryId) {
    id
    name

    character {
      slug
      longName
    }

    moves {
      ...move
    }
  }
}