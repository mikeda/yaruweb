mutation CreateVideoComment($videoId: ID!, $attributes: VideoCommentAttributes!) {
  createVideoComment(input: {videoId: $videoId, attributes: $attributes}) {
    videoComment {
      ...videoComment
    }
  }
}
